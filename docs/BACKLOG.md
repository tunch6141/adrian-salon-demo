# Future implementation

## Revise an already approved cleaning decision

Deferred at the owner's request on 20 September 2026. Add a Streamlit flow to select an existing approved rule, review its affected records and propose a replacement (for example S Wong mapped to Sam instead of Sarah). Require owner confirmation, save the reason and approver, append a superseding approval and regenerate the active cleaned version. Preserve original raw data and all earlier versions and approvals. Explain whether the change affects the current dataset and future matching imports. Do not implement this as part of the commercial reasoning patch.
